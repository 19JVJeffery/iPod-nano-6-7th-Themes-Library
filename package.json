{
  "name": "ipod-nano-themes-library",
  "private": true,
  "version": "1.0.0",
  "description": "GitHub Pages-hosted iPod nano themes library with moderated IPSW submissions.",
  "scripts": {
    "check": "node --check site.js && node --check submit.js && node --check admin.js && node --check scripts/import-approved.js && node --check lfs-worker/src/worker.mjs",
    "import:approved": "node scripts/import-approved.js",
    "lfs:set-secrets": "bash ./scripts/set-lfs-backend-secrets.sh",
    "lfs:deploy": "cd lfs-worker && wrangler deploy --config wrangler.toml"
  },
  "dependencies": {}
}
